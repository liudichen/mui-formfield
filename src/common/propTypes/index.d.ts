import PropTypes from 'prop-types';

import { fieldWrapperPropTypesType } from '../../types';

declare const sx: PropTypes.Requireable<object>;
declare const fieldWrapperPropTypes: fieldWrapperPropTypesType;
export { sx, fieldWrapperPropTypes };
